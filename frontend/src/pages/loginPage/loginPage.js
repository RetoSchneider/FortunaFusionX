import React from 'react';
import LoginForm from '../../components/loginPage/loginForm';

const LoginPage = () => {
    return (
        <div>
            <LoginForm />
        </div>
    );
}

export default LoginPage;
